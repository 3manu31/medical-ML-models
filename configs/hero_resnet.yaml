model:
  name: "resnet18"
  
training:
  epochs: 30
  batch_size: 32
  learning_rate: 0.0001
  # Dynamic weight decay to control regularization of fine-tuned weights
  weight_decay: 0.01
  optimizer: "adam"
  num_workers: 0
  seed: 42
